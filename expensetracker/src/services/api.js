// src/services/api.js
// Placeholder API service for future backend integration.
// Currently unused because app is frontend-only using localStorage.
const api = {
  // optional: wrapper functions if you later add backend
};

export default api;
